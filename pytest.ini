[pytest]
testpaths = tests
# Without this the spc package is only importable when pytest happens to be
# started as "python -m pytest", which puts the working directory on sys.path.
# The bare "pytest" entry point does not, and that is what CI runs.
pythonpath = .
addopts = -ra --strict-markers
